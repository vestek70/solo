import { identifierModuleUrl } from "@angular/compiler";

export class Categories {
    id: number = id;
    title: string;
    constructor(id: number,
        title: string){
            this.id = id;
            this.title = title;

        }
}
